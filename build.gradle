buildscript {
    dependencies {
        classpath 'org.asciidoctor:asciidoctorj-epub3:1.5.0-alpha.8.1'
        classpath 'org.asciidoctor:asciidoctorj-groovy-dsl:1.6.0-alpha.1'
    }
}

plugins {
    id "org.asciidoctor.convert" version "1.5.7"
}

repositories {
    mavenLocal()
    jcenter()
}

asciidoctorj {
    version = '1.6.0-alpha.6'
}

asciidoctor.outputs.upToDateWhen { false }

asciidoctor {
    backends 'html5', 'epub3'
    sourceDir = file('src/docs/asciidoc')
    sources {
        include project.ext.file
    }
    outputDir = file('build/')

    extensions {
    }
}
